-r requirements.txt

# --- Model conversion (one-time setup only, not needed at runtime) ---
# ct2-transformers-converter requires torch to load the source Marian weights.
torch==2.8.0

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1

# --- Benchmarking / accuracy measurement ---
jiwer==3.1.0
sacrebleu==2.5.1

# --- Lint / format ---
ruff==0.9.2
